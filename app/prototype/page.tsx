import { PrototypeHome } from '@/app/page';

export const metadata = { title: '游民产品原型基线', robots: { index: false, follow: false } };

export default function PrototypePage() {
  return <PrototypeHome />;
}
